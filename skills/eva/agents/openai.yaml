interface:
  display_name: "EVA Title Cards"
  short_description: "Handle /eva and render original title-card layouts."
  default_prompt: "Use $eva to handle /eva --help or render an EVA title card."
